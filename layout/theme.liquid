<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>PHENEX - Streetwear Collection</title>
    <meta name="description" content="PHENEX - Streetwear meets modern minimalism" />
    <meta name="Marouane Sadoune, Abdsamad sbaar" content="PHENEX" />

    <!-- Open Graph tags -->
    <meta property="og:title" content="PHENEX" />
    <meta property="og:description" content="Streetwear meets modern minimalism" />
    <meta property="og:type" content="website" />
    
    <!-- CSS -->
    {{ 'application.css' | asset_url | stylesheet_tag }}
    
    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@400;500;600;700;800&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
  </head>

  <body>
    {% section 'navbar' %}
    <main class="pt-16">
      {% section 'hero-section' %}
      {% section 'product-grid' %}
      {% section 'footer' %}
    </main>
  </body>
</html>